# Netlify deployment config
# Connect konduyt-web repo in netlify.com → deploy
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
