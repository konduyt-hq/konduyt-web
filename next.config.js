/** @type {import('next').NextConfig} */
const nextConfig = {
  // Required for Cloudflare Pages deployment
  // @cloudflare/next-on-pages handles the edge runtime conversion
}

module.exports = nextConfig
